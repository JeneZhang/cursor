# PRD：Cursor Multitask（并行多任务）

**版本：** 草稿 v0.1  
**状态：** 基于现有产品能力整理  
**目标用户：** 在 Cursor 中同时推进多项独立工作的开发者

---

## 1. 背景与问题

当前 Agent 默认是**串行队列**：一条请求做完，下一条才开始。长任务会占住主对话，后续提问只能排队。

用户需要：不挡主会话、能把**互不依赖**的工作同时跑起来。

## 2. 产品目标

| 目标 | 说明 |
|------|------|
| 不阻塞主对话 | 新请求可进后台子 Agent，主线程可继续说话 |
| 并行加速 | 独立工作流同时执行，缩短总等待 |
| 主会话干净 | 子 Agent 自带上下文，中间过程不上屏刷屏 |
| 有序不乱 | 有依赖的步骤仍按序执行 |

**非目标：** 自动解决同一文件上的并发冲突；替代单线程的精细实现/审查。

## 3. 功能概述

Multitask 把工作交给**异步子 Agent 并行执行**，而不是排成一条队。

核心行为：

1. **请求并行化**：新 prompt 可变成后台任务，不必等当前跑完。
2. **大任务拆分**：大需求可拆成多块，由子 Agent 同时做。
3. **队列加速**：已排队消息可改为并行启动。
4. **计划内并行**：Plan 里 **Build in Parallel** — 独立步骤并行，依赖步骤串行。

每个子 Agent：**独立上下文**、相对独立工作、完成后回主对话。

## 4. 用户入口

| 入口 | 操作 |
|------|------|
| Slash | `/multitask` + 任务描述 |
| Plan | 打开计划 → **Build in Parallel** |
| 队列 | 对已排队消息说「用 multitask 并行跑」 |
| 管理 | Agents Window（`Cmd+Shift+P` → Open Agents Window） |

示例：

```text
/multitask 给 billing 写单测，同时补 auth middleware 文档
```

可选后续（3.3）：**Split changes into PRs**，把结果拆成多个 PR。

## 5. 使用边界

**适合**

- 工作流互相独立（不同模块测试、不同目录文档、不相关重构）
- 不想被长任务堵住后续提问
- Plan 里已有可并行的独立步骤

**不适合**

- 多任务会改**同一批文件**
- 必须「设计 → 实现 → 验证」共享上下文的一条链
- 需要逐步盯每一处改动的审查

## 6. 风险：文件碰撞

两个任务同时改同一路径（或强耦合文件），类似两人无同步改同一份文档：**后写入覆盖先写入**，或合成一份两边都不对的代码。

缓解：只并行独立目录/模块；重叠改动串行；高风险场景用 **worktrees / 隔离 checkout**。

## 7. 成功标准（建议）

- 开启后主对话可继续发消息，不必等后台结束。
- 独立任务可同时跑，总耗时明显低于纯串行。
- 主会话只收完成摘要，不刷中间日志。
- 有依赖的步骤不会被错误地并行。

## 8. 范围外 / 后续

- 自动检测「会撞文件」并拦截或改串行
- 更细的冲突 UI 与合并策略
- 与 worktrees 的一键绑定

---

一句话：**Multitask = 主对话不排队，独立活交给后台子 Agent 一起干；同一文件别并行。**
